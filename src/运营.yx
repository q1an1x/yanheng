引「统计.yx」为 统计；

法 毫秒文（所典：典，键：文）：文 则
    若 有键（所典，键）且（所典【键】是 数）则 归 统计.整数文（所典【键】）；终
    归「--」；
终

公 法 TTFT摘要（概览：典）：典 则
    若 非 有键（概览，「ttft」）或 非（概览【「ttft」】是 典）则
        归 {「p99」：「--」,「detail」：「暂无首 Token 延迟数据」}；
    终
    定 TTFT：典 为 概览【「ttft」】；
    令 有数据：理 为 假；
    逐 键 于【「p99_ms」,「p95_ms」,「p90_ms」,「p50_ms」,「avg_ms」,「max_ms」】则
        若 有键（TTFT，键）且（TTFT【键】是 数）则 置 有数据 为 真；终
    终
    若 非 有数据 则 归 {「p99」：「--」,「detail」：「暂无首 Token 延迟数据」}；终
    定 细节：文 为「P95  」加 毫秒文（TTFT，「p95_ms」）加「 ms  ·  P90  」加 毫秒文（TTFT，「p90_ms」）加「 ms  ·  P50  」加 毫秒文（TTFT，「p50_ms」）加「 ms\nAvg  」加 毫秒文（TTFT，「avg_ms」）加「 ms  ·  Max  」加 毫秒文（TTFT，「max_ms」）加「 ms」；
    归 {「p99」：毫秒文（TTFT，「p99_ms」）,「detail」：细节}；
终

公 法 健康摘要（概览：典）：典 则
    若 非 有键（概览，「health_score」）或 非（概览【「health_score」】是 数）则
        归 {「score」：-1,「score_text」：「-- / 100」,「progress」：0,「status」：「暂无数据」}；
    终
    定 分数：数 为 概览【「health_score」】；
    若 分数 小于 0 或 分数 大于 100 则 归 {「score」：-1,「score_text」：「-- / 100」,「progress」：0,「status」：「暂无数据」}；终
    令 状态：文 为「异常」；
    若 分数 大于等于 60 则 置 状态 为「风险」；终
    若 分数 大于等于 90 则 置 状态 为「健康」；终
    归 {「score」：分数,「score_text」：统计.整数文（分数）加「 / 100」,「progress」：分数 除 100,「status」：状态}；
终
